# Segredos reais — NUNCA versionar. Só o .env.example vai pro git.
.env
*.local.env
k8s/**/overlays/local/.env
k8s/**/overlays/*/.env

# Ferramentas / temporários
*.log
.DS_Store
kubeconfig
*.kubeconfig

# Gerado por scripts/aws-render.sh a partir do `terraform output` (tem senhas)
k8s/**/overlays/aws/generated/
# Terraform
.terraform/
*.tfstate
*.tfstate.*
terraform/aws/backend.hcl
