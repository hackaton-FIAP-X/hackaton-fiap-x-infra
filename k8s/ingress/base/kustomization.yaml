apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PLT-3 — roteamento por host único.
# O controller (ingress-nginx) é instalado pelo script, não por este overlay.
resources:
  - ingress.yaml
